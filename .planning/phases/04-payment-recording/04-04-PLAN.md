---
phase: 04-payment-recording
plan: "04"
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified:
  - src/pages/MortgagePaymentsPage.jsx
  - src/components/MortgagePaymentFormDialog.jsx
  - src/main.jsx
autonomous: true
requirements: [MORT-04, MORT-05, MORT-06]

must_haves:
  truths:
    - "User navigates to /payments/mortgage and sees a page to record mortgage payments"
    - "User selects a mortgage from a dropdown and picks a month"
    - "User enters paid date and amount, saves, and the record appears with on-time or late badge"
    - "If a record exists for that mortgage+month the dialog shows an inline overwrite warning"
    - "User can clear an existing mortgage payment record via Clear payment button"
  artifacts:
    - path: "src/pages/MortgagePaymentsPage.jsx"
      provides: "Mortgage payment entry page with mortgage/month selectors and records list"
    - path: "src/components/MortgagePaymentFormDialog.jsx"
      provides: "Modal: paid_date + amount_paid inputs, inline duplicate confirm, clear button"
    - path: "src/main.jsx"
      provides: "Route /payments/mortgage wired to MortgagePaymentsPage"
  key_links:
    - from: "MortgagePaymentFormDialog.jsx"
      to: "/api/mortgage-payments POST and DELETE"
      via: "fetch in handleSubmit and handleDelete"
      pattern: "fetch.*mortgage-payments"
    - from: "MortgagePaymentsPage.jsx"
      to: "/api/mortgages"
      via: "useEffect on mount to populate mortgage dropdown"
      pattern: "fetch.*mortgages"
---

<objective>
Build the mortgage payment entry UI: a page where the user selects a mortgage and month,
records a payment date and amount, and sees existing records with on-time or late status.
Mirrors the rent payment UI pattern (04-02). Implements inline duplicate guard and clear-record.

Purpose: Closes out MORT-04, MORT-05, MORT-06. All payment recording is done after this plan.

Output: src/pages/MortgagePaymentsPage.jsx, src/components/MortgagePaymentFormDialog.jsx,
        updated src/main.jsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/main.jsx
@src/pages/PropertiesPage.jsx
@src/components/PropertyFormDialog.jsx
@src/components/DeleteConfirmDialog.jsx
@.planning/phases/04-payment-recording/04-02-SUMMARY.md

<interfaces>
API surface from 04-03:

  GET  /api/mortgage-payments?mortgage_id=N  returns MortgagePayment[]
  GET  /api/mortgage-payments?month=YYYY-MM  returns MortgagePayment[] with lender, property_address
  POST /api/mortgage-payments                body: { mortgage_id, month, paid_date, amount_paid }
                                             returns 201 MortgagePayment
  DELETE /api/mortgage-payments/:id          returns { deleted: true }

  MortgagePayment: { id, mortgage_id, month, paid_date, amount_paid, is_on_time, created_at }

Existing mortgages API:

  GET /api/mortgages  returns [{ id, property_id, lender, due_day, amount,
                                 property_address, created_at }]

shadcn/ui available: Button, Card, Badge, Dialog family, Input, Label.
Plain HTML select with Tailwind for dropdowns (established pattern from 2026-04-08).

RentPaymentsPage.jsx and RentPaymentFormDialog.jsx (just built in 04-02) are the
direct models for this plan — mirror them exactly, substituting mortgage concepts.
Key differences:
  - No property pre-filter step: mortgage dropdown shows all mortgages (lender + property_address)
  - No grace period in on-time display (is_on_time already computed server-side, just display it)
  - Dialog needs no changes to on-time logic — it just POSTs and reads is_on_time from response
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MortgagePaymentFormDialog with inline duplicate guard and clear button</name>
  <files>src/components/MortgagePaymentFormDialog.jsx</files>
  <action>
Create src/components/MortgagePaymentFormDialog.jsx. Mirror RentPaymentFormDialog.jsx
exactly, substituting mortgage for tenant/rent concepts.

Props:
  open           boolean
  onOpenChange   fn(bool)
  mortgage       { id, lender, property_address } or null
  month          string YYYY-MM
  existingRecord { id, paid_date, amount_paid, is_on_time } or null
  onSaved        fn(savedRecord)
  onDeleted      fn()

State: paidDate, amountPaid, error, saving, showDuplicateConfirm (same as rent version)

useEffect([open, existingRecord]):
  Same reset/populate logic as RentPaymentFormDialog.

handleSubmit(e):
  Same flow as RentPaymentFormDialog:
  1. e.preventDefault(), validate paidDate regex and amount > 0
  2. If existingRecord non-null AND showDuplicateConfirm false: set showDuplicateConfirm(true), return
  3. POST /api/mortgage-payments with { mortgage_id: mortgage.id, month, paid_date, amount_paid }
  4. onSaved(saved), onOpenChange(false)

handleDelete():
  DELETE /api/mortgage-payments/:existingRecord.id
  If ok: onDeleted(), onOpenChange(false)
  Else: setError(...)

Layout identical to RentPaymentFormDialog with these substitutions:
  - Subtitle: "{mortgage?.lender} — {mortgage?.property_address} — {month}"
  - Duplicate warning text: "A payment for {mortgage.lender} in {month} already exists..."
  - DialogTitle: existingRecord ? 'Edit Mortgage Payment' : 'Record Mortgage Payment'
  - Button labels: "Record mortgage payment" / "Update mortgage payment" / "Overwrite mortgage payment"
  - Clear button label: "Clear payment"

Use same imports: Dialog family from '@/components/ui/dialog', Button, Input, Label.
  </action>
  <verify>
    <automated>node --input-type=module -e "import fs from 'fs'; const s = fs.readFileSync('src/components/MortgagePaymentFormDialog.jsx','utf8'); const ok = s.includes('showDuplicateConfirm') && s.includes('/api/mortgage-payments') && s.includes('onDeleted') && s.includes('mortgage_id'); console.log(ok ? 'PASS' : 'FAIL'); if (!ok) process.exit(1);"</automated>
  </verify>
  <done>
    - MortgagePaymentFormDialog.jsx exists with all six props
    - Inline duplicate confirm flow works (two submits required to overwrite)
    - Clear payment button DELETEs the record and calls onDeleted
    - Subtitle shows lender, property address, and month
  </done>
</task>

<task type="auto">
  <name>Task 2: MortgagePaymentsPage with mortgage/month selectors, records list, route</name>
  <files>src/pages/MortgagePaymentsPage.jsx, src/main.jsx</files>
  <action>
Create src/pages/MortgagePaymentsPage.jsx. Mirror RentPaymentsPage.jsx with mortgage-specific
simplification: no property pre-filter — mortgage dropdown shows all mortgages directly.

State:
  mortgages []         selectedMortgageId string
  selectedMonth string (default current YYYY-MM)
  records []           loading boolean
  formOpen boolean     apiError string|null

Derived: selectedMortgage = mortgages.find(m => m.id === Number(selectedMortgageId)) or null
Derived: existingRecord = records.find(r => r.month === selectedMonth) or null

On mount (useEffect []): fetch /api/mortgages. setMortgages. Default selectedMonth to
  new Date().toISOString().slice(0, 7)

When selectedMortgageId changes (useEffect [selectedMortgageId]):
  If set: fetch /api/mortgage-payments?mortgage_id=N and setRecords.
  Else: setRecords([]).

Layout:

  Page title: "Record Mortgage Payment" h2 font-semibold

  Selector row (2 plain HTML selects, flex gap-4):
    1. Mortgage dropdown: value=selectedMortgageId, onChange=setSelectedMortgageId
         option value="" disabled: "Select mortgage"
         mortgages.map: option value=m.id "{m.lender} — {m.property_address}"
    2. Month input: type="month" value=selectedMonth onChange=setSelectedMonth

  Record Payment button: disabled when no mortgage selected. On click setFormOpen(true).
    Button text: existingRecord ? 'Edit Payment' : 'Record Payment'

  apiError banner (same pattern as other pages)

  Records section (when selectedMortgageId is set):
    Heading: "{selectedMortgage?.lender} — Payment History"
    Table or card list sorted by month DESC:
      Columns: Month | Paid Date | Amount | Status
      Status badge: is_on_time === 1 => Badge "On Time" (default variant)
                    is_on_time === 0 => Badge "Late" (destructive variant)
    Empty state: "No payments recorded yet for this mortgage."

  MortgagePaymentFormDialog:
    open=formOpen onOpenChange=setFormOpen
    mortgage=selectedMortgage month=selectedMonth
    existingRecord=existingRecord
    onSaved=() => { setFormOpen(false); re-fetch records for mortgage }
    onDeleted=() => { setFormOpen(false); re-fetch records for mortgage }

Style selects: same Tailwind classes as RentPaymentsPage
  "border rounded px-3 py-2 text-sm bg-background focus:outline-none focus:ring-1 focus:ring-ring"

Update src/main.jsx:
  Import MortgagePaymentsPage from './pages/MortgagePaymentsPage'
  Add route: { path: 'payments/mortgage', element: <MortgagePaymentsPage /> }
  (Keep existing /payments/rent route; keep /payments as RentPaymentsPage default)
  </action>
  <verify>
    <automated>node --input-type=module -e "import fs from 'fs'; const s = fs.readFileSync('src/pages/MortgagePaymentsPage.jsx','utf8'); const ok = s.includes('selectedMortgageId') && s.includes('MortgagePaymentFormDialog') && s.includes('/api/mortgage-payments') && s.includes('existingRecord'); const m = fs.readFileSync('src/main.jsx','utf8'); const mok = m.includes('MortgagePaymentsPage') && m.includes('payments/mortgage'); console.log((ok && mok) ? 'PASS' : 'FAIL: page=' + ok + ' main=' + mok); if (!ok || !mok) process.exit(1);"</automated>
  </verify>
  <done>
    - MortgagePaymentsPage.jsx exists with mortgage and month selectors
    - Selecting a mortgage fetches and displays that mortgage's payment history
    - MortgagePaymentFormDialog wired with correct props
    - src/main.jsx has route /payments/mortgage pointing to MortgagePaymentsPage
    - On Time / Late badges render based on is_on_time from API
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User form input to POST /api/mortgage-payments | paid_date and amount_paid cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-17 | Spoofing | MortgagePaymentFormDialog POST body | mitigate | Client-side regex on paidDate before submit; server-side validation in 04-03 is authoritative |
| T-04-18 | Tampering | duplicate overwrite flow | mitigate | Two-submit gate (showDuplicateConfirm) prevents accidental overwrite |
| T-04-19 | Information Disclosure | apiError shown to user | accept | Generic user-facing strings only; no stack traces rendered |
| T-04-20 | DoS | records fetch on mortgage selection change | accept | Local app; small dataset; no debounce needed |
</threat_model>

<verification>
1. Visit http://localhost:5173/payments/mortgage — page renders without console errors
2. Mortgage dropdown populates with all mortgages showing "Lender — Property Address"
3. Select a mortgage — payment history section appears (empty if none recorded)
4. Click Record Payment — dialog opens with today's date pre-filled
5. Submit valid data — record appears in list with On Time or Late badge
6. Click Record Payment again for same month — inline duplicate warning appears on first submit;
   clicking the submit button again overwrites the record
7. Open dialog and click Clear payment — record is removed from list
8. Visit http://localhost:5173/payments/rent — still works (both routes coexist)
</verification>

<success_criteria>
- /payments/mortgage renders mortgage and month selectors with payment history list
- MortgagePaymentFormDialog opens for Record/Edit, inline warning on overwrite attempt
- Clear payment button deletes the record and refreshes the list
- On Time / Late badges display correctly based on is_on_time from API (no grace period)
- Both /payments/rent and /payments/mortgage routes work independently
</success_criteria>

<output>
After completion, create `.planning/phases/04-payment-recording/04-04-SUMMARY.md`
</output>
